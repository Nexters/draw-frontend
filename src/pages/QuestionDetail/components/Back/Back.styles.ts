import styled from '@emotion/styled';

const BackContainer = styled.div`
  width: 100%;
  height: 32px;
  display: flex;
  justify-content: flex-start;
  align-items: center;
  padding: 0px 20px;
`;

const BackButton = styled.button`
  display: flex;
  align-items: center;
  justify-content: center;
`;

export default { BackButton, BackContainer };
