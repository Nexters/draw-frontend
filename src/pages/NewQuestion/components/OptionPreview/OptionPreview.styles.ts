import styled from '@emotion/styled';

const OptionPreviewContainer = styled.div`
  height: 60px;
  width: 100%;

  display: flex;
  align-items: center;
  justify-content: flex-start;
  padding-left: 24px;
  gap: 12px;
`;

export default { OptionPreviewContainer };
